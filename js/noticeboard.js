const c=window.KURDUWADI_CONFIG;const db=window.supabase.createClient(c.SUPABASE_URL,c.SUPABASE_ANON_KEY);const labels={birth:'👶 जन्म',marriage:'💍 विवाह',achievement:'🏅 यश',obituary:'🕯️ श्रद्धांजली',lost_found:'🔍 हरवले/सापडले',other:'📌 इतर'};const q=new URLSearchParams(location.search).get('type')||'all';document.querySelectorAll('.notice-tabs a').forEach(a=>{if(a.dataset.type===q)a.classList.add('active')});function esc(v){return String(v??'').replace(/[&<>'"]/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[x]))}async function load(){let query=db.from('noticeboard').select('*').eq('status','approved').eq('published',true).order('notice_date',{ascending:false}).order('created_at',{ascending:false});if(q!=='all')query=query.eq('type',q);const {data,error}=await query;const el=document.getElementById('noticeGrid');if(error){el.innerHTML='<div class="empty">माहिती लोड करता आली नाही.</div>';return}if(!data?.length){el.innerHTML='<div class="empty">या प्रकारात सध्या कोणतीही प्रकाशित नोंद नाही.</div>';return}el.innerHTML=data.map(n=>`<article class="notice-card">${n.image_url?`<img src="${esc(n.image_url)}" alt="${esc(n.title)}">`:''}<span class="notice-type">${labels[n.type]||'📌 इतर'}</span><h3>${esc(n.title)}</h3><p>${esc(n.description)}</p>${n.person_name?`<div class="notice-meta">👤 ${esc(n.person_name)}</div>`:''}${n.notice_date?`<div class="notice-meta">📅 ${esc(n.notice_date)}</div>`:''}</article>`).join('')}load();
