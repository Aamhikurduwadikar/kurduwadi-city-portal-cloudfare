(function(){'use strict';
const cfg=window.KURDUWADI_CONFIG||{};
const db=window.supabase&&cfg.SUPABASE_URL&&cfg.SUPABASE_ANON_KEY?window.supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_ANON_KEY):null;
const esc=v=>String(v??'').replace(/[&<>\'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const bs='display:inline-flex!important;visibility:visible!important;align-items:center;justify-content:center;min-height:44px!important;padding:10px 18px!important;margin:8px 8px 4px 0!important;border:0!important;border-radius:11px!important;font-weight:800!important;cursor:pointer!important;box-shadow:0 3px 8px rgba(0,0,0,.12)!important';
function controls(table,id){return '<div class="approval-actions" data-fixed="1" style="margin-top:10px"><button type="button" data-approval-table="'+esc(table)+'" data-approval-id="'+esc(id)+'" data-approval-status="approved" style="'+bs+';background:#087f5b!important;color:#fff!important">✅ मंजूर करा</button><button type="button" data-approval-table="'+esc(table)+'" data-approval-id="'+esc(id)+'" data-approval-status="rejected" style="'+bs+';background:#b42318!important;color:#fff!important">❌ नकार</button><button type="button" data-approval-table="'+esc(table)+'" data-approval-id="'+esc(id)+'" data-approval-status="deleted" style="'+bs+';background:#475569!important;color:#fff!important">🗑️ कायमचे Delete</button></div>'}
async function moderate(table,id,status,b){if(!db){alert('❌ Supabase connection उपलब्ध नाही.');return}if(!id){alert('❌ Record ID मिळाला नाही.');return}const noun=table==='profiles'?'नागरिक प्रोफाइल':'बातमी';const label=status==='approved'?'मंजूर':status==='rejected'?'नकार':'कायमचे Delete';if(!confirm(status==='deleted'?`ही ${noun} कायमची delete करायची का? ही कृती परत करता येणार नाही.`:`ही ${noun} ${label} करायची का?`))return;b.disabled=true;try{let r;if(status==='deleted'){r=await db.from(table).delete().eq('id',id).select('id').maybeSingle();if(r.error)throw r.error;if(!r.data)throw Error('नोंद delete झाली नाही.')}else{const patch=table==='profiles'?{status}:{status,published:status==='approved'};r=await db.from(table).update(patch).eq('id',id).select('id,status'+(table==='news'?',published':'')).maybeSingle();if(r.error)throw r.error;if(!r.data||r.data.status!==status)throw Error('नोंद update झाली नाही.')}alert('✅ '+label+' यशस्वी.');if(typeof window.showDashboard==='function')await window.showDashboard();else location.reload()}catch(e){console.error('Moderation:',e);alert('❌ '+label+' अयशस्वी: '+(e?.message||e));b.disabled=false}}
function inject(container,table){if(!container)return;container.querySelectorAll('.admin-item').forEach(item=>{const fixed=item.querySelector('.approval-actions[data-fixed="1"]');let source=item.querySelector('button[data-table]');let id=source?.dataset?.id||fixed?.querySelector('[data-approval-id]')?.dataset?.approvalId;if(!id)return;if(fixed){item.querySelectorAll('button[data-table]').forEach(x=>x.remove());return}item.querySelectorAll('button[data-table]').forEach(x=>x.remove());item.querySelectorAll('.approval-actions').forEach(x=>x.remove());item.insertAdjacentHTML('beforeend',controls(table,id))})}
function run(){inject(document.getElementById('profiles'),'profiles');inject(document.getElementById('news'),'news')}
document.addEventListener('click',e=>{const b=e.target.closest('button[data-approval-table]');if(b){e.preventDefault();e.stopImmediatePropagation();moderate(b.dataset.approvalTable,b.dataset.approvalId,b.dataset.approvalStatus,b)}});
function start(){run();['profiles','news'].forEach(id=>{const el=document.getElementById(id);if(el)new MutationObserver(()=>requestAnimationFrame(run)).observe(el,{childList:true,subtree:true})})}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',start,{once:true}):start();
})();
